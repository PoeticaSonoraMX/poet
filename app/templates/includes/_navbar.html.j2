<nav class="navbar navbar-default">
    <div class="search-bar-margin">
        <div class="navbar-header">
            <img class="navbar-brand" src="https://poeticasonora.unam.mx/wp-content/uploads/2022/01/logoLRa.png" alt="Poética Sonora">
            <div class="navbar-brand show-mobile" style="float: right;">
                <button class="navbar-toggler borderless" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
                        aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="glyphicon glyphicon-menu-hamburger"></span>
                </button>
            </div>
        </div>

        <div id="navbarSupportedContent" class="collapse navbar-collapse">
            <ul class="nav navbar-nav">
                <li><a href="{{ url('home') }}">{{ _('Home') }}</a></li>
                <li><a href="https://poeticasonora.unam.mx/acerca-del-rda/">{{ _('About') }}</a></li>
            </ul>
            <ul class="nav navbar-nav navbar-right">
                {% if not user.is_authenticated %}
                <li><a href="{{ url('admin:index') }}">{{ _('Login') }}</a></li>
                {% else %}
                <li><a href="{{ url('admin:index') }}">{{ _('Admin') }}</a></li>
                <li><a href="{{ url('admin:logout') }}">{{ _('Logout') }}</a></li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>

